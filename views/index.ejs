<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Ethereum Wallet Input</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;500;700&display=swap');

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Outfit', sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle at top left, #ff77e9, #7a5cff, #2dd4bf);
      color: #fff;
    }

    .main {
      background: rgba(255, 255, 255, 0.1);
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0px 8px 25px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      gap: 1rem;
      width: 90%;
      max-width: 400px;
      backdrop-filter: blur(12px);
    }

    .main label {
      font-size: 1.1rem;
      font-weight: 500;
    }

    .main input {
      padding: 0.8rem 1rem;
      font-size: 1rem;
      border: none;
      border-radius: 0.6rem;
      outline: none;
      background: rgba(255, 255, 255, 0.2);
      color: #fff;
      transition: 0.3s;
    }

    .main input::placeholder {
      color: rgba(255, 255, 255, 0.6);
    }

    .main input:focus {
      background: rgba(255, 255, 255, 0.3);
      box-shadow: 0 0 12px rgba(255, 119, 233, 0.6);
    }

    /* 🔘 Submit button */
    .main button {
      padding: 0.9rem 1rem;
      font-size: 1rem;
      font-weight: 600;
      border: none;
      border-radius: 0.6rem;
      cursor: pointer;
      background: linear-gradient(135deg, #ff77e9, #7a5cff, #2dd4bf);
      color: #fff;
      transition: 0.3s;
      text-shadow: 0 1px 2px rgba(0,0,0,0.2);
    }

    .main button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.3);
    }

    .main button:active {
      transform: translateY(0);
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>
<body>
 <form action="/wallet" method="POST" class="main">
  <label for="walletaddress">Ethereum Address</label>
  <input type="text" id="walletaddress" name="value" placeholder="0x1234..." />
  <button type="submit">Submit</button>
</form>


<!-- <script>
  const input = document.getElementById("walletaddress");
  const button = document.querySelector("button");

  button.addEventListener("click",async () => {
    await SendWalletAddress()
  });

  async function SendWalletAddress(){
    let value = input.value
    const options = {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({value})
    }; 
    const request = await fetch("http://localhost:3000/wallet",options)
  }
</script> -->
</html>
